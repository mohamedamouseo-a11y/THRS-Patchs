diff --git a/frontend/src/components/Settings/DatabaseBackupTab.jsx b/frontend/src/components/Settings/DatabaseBackupTab.jsx
--- a/frontend/src/components/Settings/DatabaseBackupTab.jsx
+++ b/frontend/src/components/Settings/DatabaseBackupTab.jsx
@@
-  const buildPayload = ({
-    includeSchedule = true,
-    includeGoogleDrive = false,
-    includeClientSecret = false
-  } = {}) => {
+  const buildPayload = ({
+    includeSchedule = true,
+    includeGoogleDrive = false,
+    includeClientSecret = false,
+    clientSecretOverride = ''
+  } = {}) => {
@@
-    const shouldSetClientSecret = includeClientSecret && Boolean(clientSecret);
+    // V10: the visible/native password input is the source of truth.
+    // Browser autofill may update the DOM without updating React state.
+    const effectiveClientSecret = String(
+      clientSecretOverride ||
+      clientSecretRef.current?.value ||
+      clientSecret ||
+      ''
+    ).trim();
+    const shouldSetClientSecret =
+      includeClientSecret && Boolean(effectiveClientSecret);
@@
       googleDrive: {
         clientId: form.googleDrive.clientId,
         redirectUri: form.googleDrive.redirectUri,
         folderId: form.googleDrive.folderId,
         folderName: form.googleDrive.folderName,
         clientSecretAction: shouldSetClientSecret ? 'set' : 'keep',
-        ...(shouldSetClientSecret ? { clientSecret } : {})
+        ...(shouldSetClientSecret
+          ? { clientSecret: effectiveClientSecret }
+          : {})
       }
     };
@@
-  const save = async ({
+  const save = async ({
     quiet = false,
     includeSchedule = true,
     includeGoogleDrive = false,
     includeClientSecret = false,
     clientSecretOverride = ''
   } = {}) => {
@@
-    const payload = buildPayload({
+    const payload = buildPayload({
       includeSchedule,
       includeGoogleDrive,
-      includeClientSecret
+      includeClientSecret,
+      clientSecretOverride
     });
@@
   const handleSave = async () => {
@@
-      const liveClientSecret = getLiveClientSecret();
+      const liveClientSecret = getLiveClientSecret();
       assertSecretForChangedClientId(liveClientSecret);
       await save({
         includeGoogleDrive: driveDirty || Boolean(liveClientSecret),
         includeClientSecret: Boolean(liveClientSecret),
         clientSecretOverride: liveClientSecret
       });
diff --git a/backend/src/services/databaseBackup.service.js b/backend/src/services/databaseBackup.service.js
--- a/backend/src/services/databaseBackup.service.js
+++ b/backend/src/services/databaseBackup.service.js
@@
   const secretAction = cleanText(incomingDrive.clientSecretAction || '', 20).toLowerCase();
 
   if (secretAction === 'set') {
     const plainSecret = String(incomingDrive.clientSecret || '').trim();
+    if (!plainSecret) {
+      throw new DatabaseBackupError(
+        'Google Client Secret is required when clientSecretAction=set.',
+        400,
+        'GOOGLE_CLIENT_SECRET_REQUIRED'
+      );
+    }
     next.googleDrive.clientSecretEnc = encryptSecret(plainSecret);
   } else if (secretAction === 'clear') {
     next.googleDrive.clientSecretEnc = '';
   }
