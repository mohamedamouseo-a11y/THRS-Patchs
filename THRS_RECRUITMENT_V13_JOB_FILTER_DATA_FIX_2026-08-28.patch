diff --git a/backend/src/controllers/recruitment.controller.js b/backend/src/controllers/recruitment.controller.js
--- a/backend/src/controllers/recruitment.controller.js
+++ b/backend/src/controllers/recruitment.controller.js
@@ -42,12 +42,25 @@ exports.getAllJobs = async (req, res, next) => {
     const { status } = req.query;
     const where = {};
     if (status) where.status = status;
-    const jobs = await prisma.job.findMany({
-      where,
-      include: { applications: { select: { id: true, status: true } } },
-      orderBy: { postedAt: 'desc' }
+    const [jobs, applicationPositions] = await Promise.all([
+      prisma.job.findMany({
+        where,
+        include: { applications: { select: { id: true, status: true } } },
+        orderBy: { postedAt: 'desc' }
+      }),
+      prisma.application.findMany({
+        where: { targetPosition: { not: null } },
+        select: { targetPosition: true },
+        distinct: ['targetPosition']
+      })
+    ]);
+    res.json({
+      success: true,
+      jobs,
+      applicationPositions: applicationPositions
+        .map(item => item.targetPosition)
+        .filter(Boolean)
     });
-    res.json({ success: true, jobs });
   } catch (error) {
     next(error);
   }
diff --git a/frontend/src/pages/Recruitment.jsx b/frontend/src/pages/Recruitment.jsx
--- a/frontend/src/pages/Recruitment.jsx
+++ b/frontend/src/pages/Recruitment.jsx
@@ -556,10 +556,11 @@ const Recruitment = () => {
   const [selectedApp, setSelectedApp] = useState(null);
   const [showDetailModal, setShowDetailModal] = useState(false);
   const [detailQueryId, setDetailQueryId] = useState(() => searchParams.get('id') || '');
   const [showJobModal, setShowJobModal] = useState(false);
   const [jobs, setJobs] = useState([]);
+  const [applicationPositions, setApplicationPositions] = useState([]);
   const [editingJob, setEditingJob] = useState(null);
   const [jobForm, setJobForm] = useState(DEFAULT_JOB_FORM);
   const [jobEditorMode, setJobEditorMode] = useState('description');
 
@@ -591,15 +592,16 @@ const Recruitment = () => {
   }));
 
   const fetchStatsAndJobs = useCallback(async () => {
     try {
       const [statsRes, jobsRes] = await Promise.all([
         api.get('/recruitment/stats'),
         api.get('/recruitment/jobs')
       ]);
       setStats(statsRes.stats || null);
       setJobs(jobsRes.jobs || []);
+      setApplicationPositions(jobsRes.applicationPositions || []);
     } catch (error) {
       console.error('Failed to fetch recruitment stats/jobs:', error);
     }
   }, []);
 
@@ -1046,15 +1048,19 @@ const Recruitment = () => {
       await api.delete(`/recruitment/jobs/${job.id}`);
       fetchStatsAndJobs();
     } catch (error) {
       uiAlert(error.message || pageCopy.deleteJobFailed);
     }
   };
 
   const jobTitleOptions = Array.from(new Set(
-    jobs.map(j => j.title).filter(Boolean).concat(positionFilter ? [positionFilter] : [])
-  ));
+    jobs.map(j => j.title)
+      .concat(applicationPositions)
+      .concat(positionFilter ? [positionFilter] : [])
+      .map(title => String(title || '').trim())
+      .filter(Boolean)
+  )).sort((a, b) => a.localeCompare(b, langKey === 'ar' ? 'ar' : 'en', { sensitivity: 'base' }));
 
   const appColumns = [
     {
       header: pageCopy.table.headers.applicant,
