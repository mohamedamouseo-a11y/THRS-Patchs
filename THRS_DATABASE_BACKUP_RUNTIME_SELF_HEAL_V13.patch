diff --git a/backend/src/services/databaseBackup.service.js b/backend/src/services/databaseBackup.service.js
--- a/backend/src/services/databaseBackup.service.js
+++ b/backend/src/services/databaseBackup.service.js
@@
 async function ensurePrivateDir(dir) {
   await fsp.mkdir(dir, { recursive: true, mode: 0o700 });
   await fsp.chmod(dir, 0o700).catch(() => null);
 }
 
-async function readJsonFile(filePath, fallback) {
+function lastGoodPath(filePath) {
+  return `${filePath}.last-good`;
+}
+
+function parseRuntimeJson(raw, filePath) {
+  const parsed = JSON.parse(raw);
+  if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) {
+    throw new Error(`Invalid runtime JSON object: ${path.basename(filePath)}`);
+  }
+  return parsed;
+}
+
+async function atomicWriteJson(filePath, value, { updateLastGood = true } = {}) {
+  await ensurePrivateDir(path.dirname(filePath));
+  const payload = `${JSON.stringify(value, null, 2)}\n`;
+
+  const writeAtomic = async (targetPath) => {
+    const tmp = `${targetPath}.${process.pid}.${crypto.randomBytes(6).toString('hex')}.tmp`;
+    await fsp.writeFile(tmp, payload, { mode: 0o600 });
+    await fsp.chmod(tmp, 0o600).catch(() => null);
+    await fsp.rename(tmp, targetPath);
+  };
+
+  await writeAtomic(filePath);
+
+  // V13: every successful application write also refreshes a known-good copy.
+  // Never recurse when restoring/writing the known-good copy itself.
+  if (updateLastGood && !filePath.endsWith('.last-good')) {
+    await writeAtomic(lastGoodPath(filePath));
+  }
+}
+
+async function seedLastGoodIfMissing(filePath, value) {
+  const knownGood = lastGoodPath(filePath);
+  try {
+    await fsp.access(knownGood, fs.constants.F_OK);
+  } catch (error) {
+    if (error.code !== 'ENOENT') throw error;
+    await atomicWriteJson(knownGood, value, { updateLastGood: false });
+  }
+}
+
+async function restoreLastGood(filePath, fallback, originalError) {
+  const knownGood = lastGoodPath(filePath);
+  try {
+    const raw = await fsp.readFile(knownGood, 'utf8');
+    const recovered = parseRuntimeJson(raw, knownGood);
+
+    // Restore the primary runtime file atomically, but do not overwrite the
+    // known-good copy during recovery.
+    await atomicWriteJson(filePath, recovered, { updateLastGood: false });
+
+    console.error(
+      `[THRS_DB_RUNTIME_SELF_HEAL] recovered=${path.basename(filePath)} ` +
+      `source=${path.basename(knownGood)} reason=${originalError?.code || originalError?.name || 'INVALID_JSON'}`
+    );
+
+    return { ...fallback, ...recovered };
+  } catch (recoveryError) {
+    throw new DatabaseBackupError(
+      `Cannot read database backup runtime file: ${path.basename(filePath)}; last-good recovery unavailable`,
+      500,
+      'RUNTIME_FILE_READ_FAILED'
+    );
+  }
+}
+
+async function readJsonFile(filePath, fallback) {
   try {
     const raw = await fsp.readFile(filePath, 'utf8');
-    return { ...fallback, ...JSON.parse(raw) };
+    const parsed = parseRuntimeJson(raw, filePath);
+    await seedLastGoodIfMissing(filePath, parsed);
+    return { ...fallback, ...parsed };
   } catch (error) {
     if (error.code === 'ENOENT') return { ...fallback };
-    throw new DatabaseBackupError(`Cannot read database backup runtime file: ${path.basename(filePath)}`, 500, 'RUNTIME_FILE_READ_FAILED');
+    return restoreLastGood(filePath, fallback, error);
   }
 }
-
-async function atomicWriteJson(filePath, value) {
-  await ensurePrivateDir(path.dirname(filePath));
-  const tmp = `${filePath}.${process.pid}.${crypto.randomBytes(6).toString('hex')}.tmp`;
-  const payload = `${JSON.stringify(value, null, 2)}\n`;
-  await fsp.writeFile(tmp, payload, { mode: 0o600 });
-  await fsp.chmod(tmp, 0o600).catch(() => null);
-  await fsp.rename(tmp, filePath);
-}
