THRS Recruitment V13 — Job Filter Data Fix

Use ONLY the repaired V13 patch commit:
f093dde84d5633bc471c4b3b793725a9a8f8759c

Patch repository:
https://github.com/mohamedamouseo-a11y/THRS-Patchs.git

Patch artifact:
THRS_RECRUITMENT_V13_JOB_FILTER_DATA_FIX_2026-08-28.patch

Goal:
Fix Recruitment > Job filter so it contains every real position used by applications, not only titles from the Job table.

Expected behavior:
- GET /recruitment/jobs continues returning the normal jobs array.
- The same response additionally returns applicationPositions collected from distinct Application.targetPosition values.
- Recruitment.jsx merges Job.title + applicationPositions + current selected position.
- Values are trimmed, empty values removed, duplicates removed, and the final list sorted.
- Existing targetPosition filtering behavior remains unchanged.
- V12 visual composition remains unchanged.

IMPORTANT HISTORY:
- Do NOT use older V13 commits 95cea2a9... or a0021927...
- The repaired patch at f093dde84d5633bc471c4b3b793725a9a8f8759c has corrected unified-diff hunk counts.
- Do NOT manually apply any hunk if checks fail.

SAFE EXECUTION:
1) Work in /var/www/tamiyouz_hr.
2) Do not reset, checkout, clean, overwrite, or delete unrelated local changes.
3) Fetch the exact patch from THRS-Patchs commit f093dde84d5633bc471c4b3b793725a9a8f8759c.
4) Existing backup may be reused if still valid, otherwise create a timestamped backup of:
   - backend/src/controllers/recruitment.controller.js
   - frontend/src/pages/Recruitment.jsx
5) Run BOTH:
   git apply --numstat /path/to/THRS_RECRUITMENT_V13_JOB_FILTER_DATA_FIX_2026-08-28.patch
   git apply --check /path/to/THRS_RECRUITMENT_V13_JOB_FILTER_DATA_FIX_2026-08-28.patch
6) Both must pass. If either fails: STOP and return exact error. Do NOT use --3way, --reject, or manual application.
7) Apply exactly once:
   git apply /path/to/THRS_RECRUITMENT_V13_JOB_FILTER_DATA_FIX_2026-08-28.patch
8) Verify only these source files changed for V13:
   - backend/src/controllers/recruitment.controller.js
   - frontend/src/pages/Recruitment.jsx
9) Build frontend with the existing production build command.
10) Restart the existing PM2 process tamiyouz-hr because backend controller code changes.
11) Do not change Prisma schema, migrations, DB data, Nginx config, DNS, SSL, ports, auth, routes, or unrelated files.
12) Smoke test:
   - https://hr.tamiyouzplaform.com/recruitment => HTTP 200
   - https://hr.tamiyouzplaform.com/ => HTTP 200
13) Validate /recruitment/jobs response contains both jobs and applicationPositions.
14) Validate the Job filter now includes positions that existed only in Application.targetPosition, including examples visible in applicant rows where present.
15) Select at least one newly surfaced position and confirm filtering still uses targetPosition and returns matching applicants.
16) Do NOT commit. Do NOT push. Leave production changes local for review.

Required report:
- patch commit actually used
- git apply --numstat output
- git apply --check output
- changed files
- frontend build result
- PM2 restart/status
- HTTP smoke results
- evidence applicationPositions is returned and consumed
- evidence formerly missing titles now appear in dropdown
- evidence selecting one works
- backup path
- final git status --short for the two V13 files
- confirmation no DB/schema/Nginx/DNS/SSL changes

FINAL_MARKER=THRS_RECRUITMENT_V13_JOB_FILTER_DATA_FIX_DEPLOYED_OK
